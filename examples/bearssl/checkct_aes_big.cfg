starting from core


replace <high_input_240> (ptr) by
  @[ptr, 240] := secret as high_input_240
  return
end

replace <high_input_32> (ptr) by
  @[ptr, 32] := secret as high_input_32
  return
end

replace <low_input_4> (ptr) by
  @[ptr, 4] := public as low_input
  return
end



explore all
cut at <main:last>
